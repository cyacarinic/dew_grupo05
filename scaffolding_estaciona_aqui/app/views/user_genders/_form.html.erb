<%= form_with(model: user_gender, local: true) do |form| %>
  <% if user_gender.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_gender.errors.count, "error") %> prohibited this user_gender from being saved:</h2>

      <ul>
      <% user_gender.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :user_gender_nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
