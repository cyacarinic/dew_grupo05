<%= form_with(model: user_type, local: true) do |form| %>
  <% if user_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_type.errors.count, "error") %> prohibited this user_type from being saved:</h2>

      <ul>
      <% user_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :user_type_nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion, id: :user_type_descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
