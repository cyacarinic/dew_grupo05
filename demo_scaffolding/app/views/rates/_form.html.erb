<%= form_with(model: rate, local: true) do |form| %>
  <% if rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rate.errors.count, "error") %> prohibited this rate from being saved:</h2>

      <ul>
      <% rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :rate_user_id %>
  </div>

  <div class="field">
    <%= form.label :parking_id %>
    <%= form.number_field :parking_id, id: :rate_parking_id %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_field :comentario, id: :rate_comentario %>
  </div>

  <div class="field">
    <%= form.label :puntuacion %>
    <%= form.number_field :puntuacion, id: :rate_puntuacion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
